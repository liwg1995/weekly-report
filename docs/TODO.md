# TODO

## 当前阶段策略

- 仅进行本地测试验证
- 生产部署延后执行

## 本地验证（持续执行）

- [x] `pnpm test` 全仓测试通过（api + web）
- [x] `pnpm --filter api test:e2e` 通过（含审批、模板、通知链路）
- [x] 模板脱敏策略（全局/模板覆盖）相关测试通过
- [x] 通知模块测试通过（开关、模板、重试、超时、钉钉签名）
- [x] 本地压测脚本可用：`pnpm --filter api benchmark:local`
- [x] 压测基线已记录：`docs/verification/2026-03-06-local-benchmark.md`
- [x] 一键本地回归命令：`pnpm regression:local`（api test + api e2e + web test + benchmark）
- [x] Web 审批页扁平化交互优化已完成（快捷导航、扁平组件样式）
- [x] 绩效考核占位已完成（`/manager/performance` + 占位数据结构）
- [x] 绩效占位已升级为 Prisma 持久化（`/api/performance/overview` + e2e）
- [x] 绩效占位可配置化已完成（创建周期/新增维度/更新待办 + 前端表单）
- [x] Web 构建编译类型错误已修复（onClick 回调签名兼容）

## 延后到生产部署阶段

- [ ] 生产环境变量与密钥托管（JWT/企业微信/钉钉 webhook）
- [ ] 生产数据库迁移与回滚演练
- [ ] 反向代理与 HTTPS（Nginx/Caddy）
- [ ] 进程守护与日志采集（PM2/systemd）
- [ ] 监控与告警（接口可用性、数据库连接、错误率）
- [ ] 企业微信/钉钉真实群机器人联调与验收

## 下一步执行项

- [ ] 给绩效配置加完整约束（建议加以下规则后再放量）
  - [x] 周期编辑时校验结束日期 >= 开始日期
  - [x] 周期删除前加二次确认（联动清除维度）
  - [x] 维度权重总和校验（创建/编辑时避免超过 100%）
  - [x] 维度 Key 唯一性与空值、长度校验的单元级覆盖
- [x] 绩效配置变更做审计日志（记录变更前后字段）
- [x] 绩效配置加入乐观并发控制（更新时带版本号）
- [x] `/manager/performance` 的提示文案与禁用态统一收口（前端体验收敛）
- [x] 准备生产发布演练清单（按 `docs/deployment/2026-03-06-release-checklist.md` 扩展）
- [x] 新增生产就绪脚本 `pnpm release:readiness`

## 前端一期状态（2026-03-20）

- [x] 登录与工作台分流：`admin/review/employee` 三类工作台入口已打通
- [x] 前端能力守卫：未授权统一进入 `403`（`/forbidden`）
- [x] 审批管理页大模块已拆分（筛选/效率面板/待审批/模板/日志/导出）
- [x] 组织管理页已拆分（部门面板 + 员工归属面板）
- [x] 绩效配置页已拆分（周期面板 + 维度面板 + 待办面板）
- [x] 全局扁平化样式收口（按钮主次层级、面板留白、标题层级、移动端筛选可用）

## 近期不做（保留占位）

- [ ] 企业微信/钉钉真实发送链路（仅保留入口与占位提示）
- [ ] 绩效模型深度联动（和周报建议分值、自动评分规则）
- [ ] 生产部署与运维编排（本阶段只做本地可运行验证）

## 本地验收步骤（建议）

1. 启停服务
   - 启动：`./scripts/dev-start.sh`
   - 停止：`./scripts/dev-stop.sh`
   - 重启：`./scripts/dev-restart.sh`
2. 前端/后端回归
   - `pnpm --filter web test`
   - `pnpm --filter web build`
   - `pnpm --filter api test`
3. 人工验收路径（按角色）
   - 管理员：组织管理 -> 绩效配置 -> 审批管理
   - 领导：审批管理（筛选/通过/驳回/建议）
   - 员工：周报提交与历史反馈
4. 关键体验点
   - 页面权限拦截正确（越权进入 403）
   - 筛选、分页、批量操作可用
   - 主操作按钮与危险操作按钮视觉区分明确

## 本次里程碑总结

- [x] 绩效周期/维度编辑与删除已通过 Prisma 落库
- [x] 绩效配置 Web 端编辑删除交互已实现
- [x] API e2e + web 测试 + 回归 `pnpm regression:local` 已通过
- [x] Web 点击事件类型兼容修复已完成，生产构建通过
